<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
    <title>Feedback Detail</title>
    <style>
        .content {
            flex: 1 0 auto;
        }

        .feedback-details {
            margin-top: 20px;
            padding: 20px;
            background-color: white;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
            border-radius: 5px;
        }

        .feedback-images img {
            width: 100%;
            max-width: 200px;
            margin-right: 10px;
            margin-bottom: 10px;
        }

        .back-button {
            display: block;
            color: grey;
            text-decoration: none;
            margin-top: 15px;
        }

        .back-button:hover {
            text-decoration: none;
            color: darkgrey;
        }
    </style>
</head>

<body>
    <%- include('partials/adminheader.ejs') %>

    <div class="content">
        <div class="container mt-4">
            <h2>Feedback</h2>
            <a href="/feedbacklist" class="back-button">&larr; Back</a>
            <div class="feedback-details">
                <p><strong>Name:</strong> <%= feedback.name %></p>
                <p><strong>Email:</strong> <%= feedback.email %></p>
                <p><strong>Message:</strong> <%= feedback.message %></p>
                <div class="feedback-images">
                    <% if (feedback.image1) { %>
                        <img src="/images/feedback/<%= feedback.image1 %>" alt="Feedback Image 1">
                    <% } %>
                    <% if (feedback.image2) { %>
                        <img src="/images/feedback/<%= feedback.image2 %>" alt="Feedback Image 2">
                    <% } %>
                    <% if (feedback.image3) { %>
                        <img src="/images/feedback/<%= feedback.image3 %>" alt="Feedback Image 3">
                    <% } %>
                </div>
            </div>
        </div>
    </div>

    <%- include('partials/footer.ejs') %>
</body>

</html>
